# EarlyGrade_predictor
A student performance predictor that regresses/classifies final grades from the UCI Student Performance dataset, comparing "with grades" vs. "early-warning" (no G1/G2) feature sets — plus a fairness check on family-background features — to flag at-risk students before their grades slip.
